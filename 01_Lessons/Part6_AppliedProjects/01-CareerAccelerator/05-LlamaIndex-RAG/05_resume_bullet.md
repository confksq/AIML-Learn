# 05 — Resume Bullet

**Primary (concise):**
> Built RAG pipelines with LlamaIndex — data-centric ingestion, VectorStoreIndex, and cited query engines running on a local Ollama backend with no paid API.

**Alternative (impact-oriented):**
> Delivered cited document-Q&A systems in LlamaIndex leveraging node-relationship (parent-child) retrieval and built-in source citations — complementing LangChain/Semantic Kernel with a RAG-specialized framework.

**Skills row additions:**
`LlamaIndex · RAG frameworks · VectorStoreIndex · query engines · source-node citations · LangChain (comparative)`

**Talking point for interviews:**
"I know RAG deeply and I've used both LangChain and LlamaIndex. LlamaIndex is my pick when the core problem is retrieval quality and cited Q&A — better indexing abstractions and citations with less code — and I use LangChain or Semantic Kernel when I need agents and orchestration around the RAG."
