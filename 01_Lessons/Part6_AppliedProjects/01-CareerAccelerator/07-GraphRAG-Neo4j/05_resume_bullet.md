# 05 — Resume Bullet

**Primary (concise):**
> Built GraphRAG pipelines with Neo4j and Cypher — LLM-driven entity/relationship extraction and multi-hop graph retrieval for questions vector search can't answer.

**Alternative (impact-oriented):**
> Delivered knowledge-graph-enhanced RAG (Neo4j + Cypher) with a demonstrated vector-vs-graph comparison, enabling multi-hop relationship reasoning and global corpus synthesis alongside a vector retrieval layer.

**Skills row additions:**
`GraphRAG · Neo4j · Cypher · knowledge graphs · Microsoft GraphRAG · hybrid retrieval (vector + graph)`

**Talking point for interviews:**
"Vector RAG is my default for semantic passage lookup, but relationship and multi-hop questions — 'which dealers share a manager who handles late accounts' — need a graph. I build knowledge graphs in Neo4j via LLM entity extraction, query them with Cypher, and run hybrid retrieval routing per query type. I only build the graph when the questions genuinely justify its construction cost."
