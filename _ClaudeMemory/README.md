# Claude Code Memory — Snapshot

Copied 2026-07-19 from:
`~/.claude/projects/-mnt-c-Users-confksq-source-repos-JMA-Apps/memory/`

**This is a snapshot, not the live memory.** Claude Code reads the path above, not this folder.
To restore on a new machine, copy these files back to that path (create the directories first).

`MEMORY.md` is the index loaded each session; the other files are individual memories.
Re-snapshot after any session where memory changed — otherwise this drifts silently.
